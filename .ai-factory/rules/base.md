# Base Rules

- Follow `.ai-factory/RULES.md` axioms for every change
- Prefer package facades over deep imports
- Keep unit tests free of Docker and PostgreSQL
- Prefer fail-closed validation for settings and migration targets
